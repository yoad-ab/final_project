import { GitFork, FileCode2 } from 'lucide-react'

// Semantic product icons — use these instead of raw lucide icons so all instances stay in sync.
export const RecipeIcon = GitFork
export const AnalysisIcon = FileCode2
